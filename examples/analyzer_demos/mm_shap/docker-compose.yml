services:
  mm_shap:
    build:
      context: ../../..
      dockerfile: examples/analyzer_demos/mm_shap/Dockerfile
    image: evalrx-mm-shap
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
