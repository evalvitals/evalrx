services:
  logprob_entropy:
    build:
      context: ../../..
      dockerfile: examples/analyzer_demos/logprob_entropy/Dockerfile
    image: evalrx-logprob-entropy
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
